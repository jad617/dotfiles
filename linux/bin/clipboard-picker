#!/bin/bash
# Open clipboard history with wofi (floats naturally via wlr-layer-shell)
"$HOME/go/bin/cliphist" list | wofi --dmenu --prompt "Clipboard" --insensitive | "$HOME/go/bin/cliphist" decode | wl-copy
